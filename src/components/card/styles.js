import {StyleSheet} from 'react-native';

import {CARD_BACKGROUND_COLOR} from '_constants/colors';

const styles = StyleSheet.create({
  card: {
    backgroundColor: CARD_BACKGROUND_COLOR,
    paddingVertical: 10,
    paddingHorizontal: 16,
    borderRadius: 8,
  },
});

export default styles;
